// Gallery masonry geometry, shared by the live Waterfall (MainSection) and the
// loading skeleton (GallerySkeleton) so the placeholder lands on exactly the
// rhythm the real grid will take.
//
// Y gap = the caption row under each thumbnail (one 2xs line + its gap) plus
// the same breathing room the columns get. PostItem's caption must fit in it.

/** Horizontal gap between columns (px). */
export const GRID_GAP = 16
/** Vertical distance between the bottom of one image and the top of the next (px). */
export const GRID_Y_GAP = 30
/** Grid padding on every side (px). */
export const GRID_PAD = 12
