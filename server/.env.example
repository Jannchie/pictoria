# Database Configuration
DB_URL=postgresql+asyncpg://user:password@localhost/pictoria

# Slow Query Logging Configuration
# Set the threshold in milliseconds for what is considered a slow query
# Default: 100ms
SLOW_QUERY_THRESHOLD_MS=100

# Optional: Set to 'true' to enable SQLAlchemy echo for all queries (very verbose)
# SQLALCHEMY_ECHO=false